"use strict";
// компонент сообщения об ошибке, будет появляться, когда не удаётся выполнить запрос к серверу

const error = {
    template:
        `
    <div class="error-data">Не удаётся выполнить запрос к серверу</div>
    `
}
